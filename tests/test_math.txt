func compute(param: integer) => integer {
    local var1: integer;
    local var2: integer;
    local var3: integer;
    local arr: integer[5];
    local total: integer;

    local var4: integer;
    local var5: integer;
    var4 := var1 + var2;
    var1 := param + var3 * 2;
    var2 := (param - 3) * (var1 + 4) / 2;
    var3 := (var2 * 7) + (12 / 2);
    var5 := var1 + var2;

    arr[0] := var1;
    arr[1] := var2 + 10;
    local tmp: integer;

    // conditional logic
    if ( tmp >= 50 ) then {
        total := total + 1;
        arr[2] := tmp;
    } else {
        total := total - 1;
    };

    /* 
    multi line comment
    */

    return (total);
};
